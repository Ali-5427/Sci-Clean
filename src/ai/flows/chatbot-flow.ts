
'use server';

/**
 * @fileOverview This flow has been removed as it was dependent on Gemini.
 */

export async function chatWithContext(input: any): Promise<string> {
  return "AI features are currently disabled.";
}
